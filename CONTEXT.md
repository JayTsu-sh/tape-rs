# LTFS 归档与接管

本词汇表描述磁带文件访问和节点故障接管中的领域概念。

## Language

**卷（Volume）**：由卷 UUID 标识的一份 LTFS 磁带文件系统；驱动器设备路径不是卷身份。

**完整索引（Full Index）**：描述卷在某一时点完整文件系统状态的 LTFS 索引快照。

**增量索引（Incremental Index）**：描述自前一份完整或增量索引以来文件系统变化的 LTFS 索引；恢复目标状态需要相应完整索引基线和其后必要的增量链。
_Avoid_: 将追加写入的一份完整索引称为增量索引，或将增量索引等同于上传回执。

**原生文件接口（Native File Interface）**：供适配应用显式调用的 Rust 文件访问接口，不要求经过操作系统挂载路径。

**透明挂载（Transparent Mount）**：普通应用通过操作系统路径访问磁带文件的入口，其兼容范围由文件操作契约规定。

**卷提交（Volume Commit）**：使一代文件数据与其可恢复索引达到约定磁带持久化保证的操作。具体完成条件由提交协议规定。
_Avoid_: 将收到写请求、缓存落盘与卷提交混称为“写入成功”。

**暂存数据（Staged Data）**：已接收但尚未完成卷提交的文件内容，其恢复保证取决于已承诺的暂存持久化级别。

**已提交状态（Committed State）**：卷上已达到提交持久化条件的文件状态及恢复所需信息，是节点接管时的权威依据。

**归档成功（Archive Success）**：应用获得的磁带持久化完成确认，对应可恢复的卷提交结果。
_Avoid_: 将上传接收完成或本地暂存完成称为归档成功。

**上传任务（Upload Task）**：应用发起的一次文件内容传送及归档意图；尚未提交的任务与磁带中的已提交文件状态分别管理。

**合批提交（Batched Commit）**：同卷多个已就绪上传共享一次卷提交，同时保留各文件的独立身份与上传结果。
_Avoid_: 将合批提交称为把多个文件打包成一个文件。

**小写入聚合（Small-write Aggregation）**：将零散的小写入汇集为较大的写入单元，不改变各文件的逻辑内容与身份。
_Avoid_: 将聚合完成等同于归档成功，或将写入单元边界等同于文件边界。

**上传结果未定（Indeterminate Upload Outcome）**：应用尚不能确定某次上传是否完成的状态，可能源于提交响应丢失，而不表示磁带上的文件系统无法恢复。
_Avoid_: 将上传结果未定等同于未提交、数据丢失或可以无条件重传。

**已提交元数据缓存（Committed Metadata Cache）**：已提交文件信息的可重建副本，用于查询和接管加速，其内容可能落后于卷的有效状态。

**设备所有者（Device Owner）**：在特定时段被授权操作目标驱动器或机械手的唯一工作主体。
_Avoid_: 将 Raft Leader 当选直接等同于设备执行就绪。

**设备执行就绪（Device Execution Readiness）**：目标受管设备的执行资格、隔离与恢复前提已满足，可开放对应设备操作的状态；控制面选举完成本身不是这一状态。

**运行许可（Runtime Permission）**：特定接管轮次和执行实例在有限时效及当前安全条件下，对指定范围副作用的执行资格。历史授权成立或曾经通过一次检查，不代表当前仍持有该资格。
_Avoid_: 将持有许可描述的副本等同于仍可执行，或将运行许可当作跨节点设备隔离的替代品。

**隔离协调许可（Fencing Coordination Permission）**：允许指定主体针对限定目标和影响范围协调隔离及核验结果的权限，不包含正常文件访问或磁带恢复操作的权限。
_Avoid_: 将有权发起隔离等同于隔离已完成或设备执行就绪。

**维护参选准入（Maintenance Candidacy Admission）**：整组停服维护中，在安全约束和真实接管前提经核验后，给予指定节点恢复正常选举参与的限定许可。
_Avoid_: 将维护参选准入等同于指定 Leader、修改投票规则或授予设备执行权。

**受管设备组（Managed Device Group）**：一个受本客户端管理的逻辑带库及分配给它的驱动器，是共同归属一个活动节点的接管范围。
_Avoid_: 默认将一台物理带库上的所有逻辑带库都视为同一受管组，或将局部故障影响范围与设备组接管范围混为一谈。

**仲裁见证（Quorum Witness）**：帮助主备节点判定行动资格的独立参与方；它不是上传数据副本，也不直接证明旧所有者已被隔离。

**计划回切（Planned Failback）**：通过受控切换将设备组所有权迁回指定节点的操作，区别于旧节点恢复上线。

**节点接管（Node Takeover）**：原工作节点故障后，另一节点获得设备所有权、恢复卷状态并恢复文件服务的过程。
_Avoid_: 将进程重启、FUSE 重新挂载等同于节点接管。

**接管轮次（Takeover Attempt）**：为受管设备组建立执行资格的一次独立接管过程；同一节点重新建立资格不等于延续旧轮次。
_Avoid_: 将接管轮次等同于 Raft 任期，或将旧轮次的就绪状态当作新轮次的执行许可。

**故障隔离（Fencing）**：确保原所有者即使恢复运行也无法继续操作受保护设备或状态的措施。

**恢复时间目标（RTO）**：从约定故障起点到约定服务能力恢复之间的目标时间；控制面恢复与磁带数据服务恢复分别计量。

**恢复点目标（RPO）**：约定故障发生后允许丢失的已确认工作量；卷提交和暂存确认分别规定保证。

**磁带池（Tape Pool）**：同一受管设备组内、同类型磁带的逻辑集合，是上传目标、容量监控与导出/回收的管理单位；池定义与磁带归属属于 Raft 复制的控制状态。
_Avoid_: 将池等同于某一盘磁带或某个目录，或将池成员变化当作普通 I/O 逐条共识。

**驱动器角色（Drive Role）**：驱动器允许承担的任务类别位组合：写入（m）、读取（r）、维护（g）。变更在当前任务完成后生效。

**任务（Task）**：由客户端或运维发起、由当前 Leader 执行实例排队派发的一次工作单元，以接管轮次标识加本轮序号为身份；状态为 waiting、running、interrupted、succeeded、failed 或 aborted。
_Avoid_: 将任务 ID 当作跨接管轮次持久的凭据，或将 aborted 解释为“未执行”。

**对账（Reconciliation）**：核对目录与磁带索引、更新可回收空间估计并可选写回磁带的维护任务；与同带写入互斥。
_Avoid_: 将对账等同于回收，或将只更新元数据的对账当作磁带已被修改。

**回收（Reclamation）**：把磁带上仍被引用的文件复制到同池另一盘磁带后重新格式化源带，以释放被删除文件占用的空间；需要两台维护角色驱动器。

**导出（Export）**与**离线（Offline）**：导出使磁带脱离池且其文件不再可访问，磁带内容不变；离线保留目录条目但标记不可访问，可通过上线恢复。
_Avoid_: 将未经导出直接取走磁带视为导出。

**磁带状态维度（Tape State Dimensions）**：管理归属、介质健康、追加资格、位置四个正交维度，替代单一扁平状态枚举；只有管理归属进入 Raft。

**文件状态（File State）**：路径当前版本的可见状态：in_progress（有活动写任务）、ready（已完整接收、尚未提交）、committed（已可靠提交并本地发布）、partial（异常结束、仅前缀已提交）、unknown（结果不明或视图不可信）。读取只服务 committed。
_Avoid_: 把 ready 或 unknown 当作“文件不存在”，或把 close 返回成功当作 committed。

**替换（Replace）**：为已存在路径创建新版本；旧版本在新版本所在索引提交发布前保持为当前版本，切换原子发生。
_Avoid_: 将替换等同于原地覆盖或追加。
